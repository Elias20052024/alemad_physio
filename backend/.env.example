# Backend Environment Variables

# Server port
PORT=5000

# Environment
NODE_ENV=development

# Database URL - Using Neon PostgreSQL (serverless)
# Get your connection strings from: https://console.neon.tech/
# 
# DATABASE_URL: Use the pooler connection (for application use)
#   Format: postgresql://[user]:[password]@[pooler-host]/[dbname]?sslmode=require
#   From Neon: Connection pooler (recommended for applications)
#
# DIRECT_URL: Use the direct connection (for migrations only)
#   Format: postgresql://[user]:[password]@[direct-host]/[dbname]?sslmode=require
#   From Neon: Direct connection (for Prisma Migrate)
#
DATABASE_URL="postgresql://neondb_owner:your_password@ep-xxxxx-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require"
DIRECT_URL="postgresql://neondb_owner:your_password@ep-xxxxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require"

# JWT Secret - Change this in production!
JWT_SECRET="your-secret-key-change-in-production"

# Email settings (optional, for future implementation)
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-password
